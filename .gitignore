*.iml
.gradle/
.idea/
build/
local.properties
.kotlin/
.cxx/
captures/

# Signing material — never commit these.
keystore.properties
*.jks
*.keystore

# Firebase config carries project identifiers; keep it out of public repos.
app/google-services.json

# Firmware credentials — WiFi SSID/password and Firebase keys.
# Commit secrets.h.example instead.
firmware/siren_esp32/secrets.h
